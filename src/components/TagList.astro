---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{tags.length > 0 && (
  <div class="tag-list">
    {tags.map((tag) => (
      <a href={`/tag/${tag}`} class="tag">#{tag}</a>
    ))}
  </div>
)}
